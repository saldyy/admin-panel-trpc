{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --watch src src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "esbuild": "0.25.9",
    "eslint": "^9.33.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@repo/user-management": "workspace:*",
    "@trpc/server": "^11.5.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^4.1.1"
  }
}
